легко

Реализуйте интерфейс `Counter` с методами `Add` и `Get`, обеспечив потокобезопасность **с помощью мьютекса**. Счётчик начинается с `0`:

- `Add(value)` увеличивает значение на переданное число;
- `Get()` возвращает текущее значение.
```go
package main

type Counter interface {

    Add(int64)

    Get() int64

}

func NewCounter() Counter {

    return nil

}
```
---
### Тесты

```go

package main

import (
    "sync"
	"testing"
)

type TestCase struct {
    name    string
    prepare func() Counter
    check   func(counter Counter) bool
}

var testCases = []TestCase{
    {
        name: "Одиночное добавление положительного числа",
        prepare: func() Counter {
            c := NewCounter()
            c.Add(42)
            return c
        },
        check: func(c Counter) bool {
            return c.Get() == 42
        },
    },
    {
        name: "Одиночное добавление отрицательного числа",
        prepare: func() Counter {
            c := NewCounter()
            c.Add(-15)
            return c
        },
        check: func(c Counter) bool {
            return c.Get() == -15
        },
    },
    {
        name: "Инициализация с нулевым значением",
        prepare: func() Counter {
            return NewCounter()
        },
        check: func(c Counter) bool {
            return c.Get() == 0
        },
    },
    {
        name: "Множественные последовательные добавления",
        prepare: func() Counter {
            c := NewCounter()
            c.Add(10)
            c.Add(20)
            c.Add(-5)
            return c
        },
        check: func(c Counter) bool {
            return c.Get() == 25
        },
    },
    {
        name: "Параллельные добавления из горутин",
        prepare: func() Counter {
            c := NewCounter()
            var wg sync.WaitGroup
            wg.Add(1000)
            for i := 0; i < 1000; i++ {
                go func() {
                    defer wg.Done()
                    c.Add(1)
                }()
            }
            wg.Wait()
            return c
        },
        check: func(c Counter) bool {
            return c.Get() == 1000
        },
    },
}

func TestSolution(t *testing.T) {
	for _, tc := range testCases {
		t.Run(tc.name, func(t *testing.T) {
			obj := tc.prepare()
			if !tc.check(obj) {
				t.Errorf("тест не пройден")
			}
		})
	}
}
```

---
### Решение
```go
package main

import "sync"

type Counter interface {
    Add(int64)
    Get() int64
}

type counter struct {
    value int64
    mu    *sync.RWMutex
}

func NewCounter() Counter {
    return &counter{
        value: 0,
        mu:    &sync.RWMutex{},
    }
}

func (c *counter) Add(value int64) {
    c.mu.Lock()
    defer c.mu.Unlock()
    c.value += value
}

func (c *counter) Get() int64 {
    c.mu.RLock()
    defer c.mu.RUnlock()
    return c.value
}
```
